name: Go Version and Release

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
    paths:
      - '.github/workflows/go-version-release.yml'
      - '.goreleaser.yml'
      - 'packaging/pj-update-skills'
  workflow_dispatch:
    inputs:
      version:
        description: 'Exact version (e.g. 1.2.3). Cannot be used with bump_type.'
        required: false
      bump_type:
        description: 'Component to bump: major | minor | patch. Cannot be used with version.'
        required: false
      version_force:
        description: 'Force specified version, ignoring strict version progression checks.'
        required: false

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      # One-time bootstrap so the release path becomes usable immediately.
      # Remove this branch-push path after v0.1.1 is published.
      - name: Check initial release
        if: github.ref_type == 'branch'
        id: bootstrap
        shell: bash
        run: |
          if git show-ref --tags --verify --quiet refs/tags/v0.1.1; then
            echo 'release=false' >> "$GITHUB_OUTPUT"
          else
            echo 'release=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Publish release
        if: github.ref_type == 'tag' || github.event_name == 'workflow_dispatch' || steps.bootstrap.outputs.release == 'true'
        uses: MiguelRodo/actions/go-version-release@509750b862eeec82bd7596b81bca13b2bdfbcc3a # current v2 action
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          version: ${{ github.ref_type == 'branch' && '0.1.1' || inputs.version }}
          bump_type: ${{ inputs.bump_type }}
          version_force: ${{ inputs.version_force }}
          goreleaser_config: .goreleaser.yml
